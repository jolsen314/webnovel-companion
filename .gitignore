node_modules/
dist/
.next/
out/
*.tsbuildinfo
coverage/
*.log
.env
.env.local
.env.prod
.DS_Store

# Claude Code local (machine-specific) settings — keep out of git
.claude/settings.local.json

# Local tooling context (Neon CLI, agent-skills caches) — not app source
.neon
.agents/
skills-lock.json
*.local.md
/local/

# Local ad-hoc curl/scratch notes (may contain secrets)
scratch

# Playwright E2E
/test-results/
/playwright-report/
/.playwright/

# Next.js generated (regenerated by dev/build; dev vs build flips the route-types path — don't track)
next-env.d.ts
