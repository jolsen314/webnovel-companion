node_modules/
dist/
.next/
out/
*.tsbuildinfo
coverage/
*.log
.env
.env.local
.DS_Store

# Claude Code local (machine-specific) settings — keep out of git
.claude/settings.local.json

# Local tooling context (Neon CLI, agent-skills caches) — not app source
.neon
.agents/
skills-lock.json
